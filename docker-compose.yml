version: '3.8'

services:
  datafeed:
    build: .
    ports:
      - "4444:4444"
    environment:
      - EXCHANGE=BINANCE  # Optional: set exchange via env var
    volumes:
      # Optional: mount a volume for persistent logs or data if needed
      # - ./logs:/app/logs
    restart: unless-stopped